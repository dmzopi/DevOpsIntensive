| NAME | PROMPT | DESCRIPTION | EXAMPLE |
| :--- | :--- | :--- | :--- |
| Single Container Pod| Create a k8s Pod manifest "app" with labels app,run set to "demo", running container "app" using image "gcr.io/k8s-k3s/demo:v1.0.0", exposing "http" port 8000. Return only valid indented yaml with start marker. | Generated by GPT-5.3, verified on Sonet-4.6 | [app.yaml](yaml/app.yaml)
| Single container Pod with livenessProbe | Create a k8s Pod manifest "app-livenessprob" in namespace "demo", running container "app" using image "gcr.io/smartcity-gl/demo:v1.0.1", exposing "http" port 8080, with livenessProbe checking "/" on port 8000 (start after 5s, check every 10s, timeout 1s, fail after 3 attempts). Return only valid indented yaml.| Generated by GPT-5.3, verified on Sonet-4.6 | [app-livenessProbe.yaml](yaml/app-livenessProbe.yaml)
| Single container pod with liveness/readiness probes| Create a k8s Pod manifest "app-readinessprob", running container "app" using image "gcr.io/smartcity-gl/harmonic:v2.0.2", exposing "http" port 8000, with livenessProbe checking "/" on port 8000 (start after 5s, check every 10s, timeout 1s, fail after 3 attempts), and readinessProbe checking "/ready" on port 8000 (check every 2s, no initial delay, mark ready after 1 success, fail after 3 attempts). Return only valid indented yaml. | Generated by GPT-5.3, verified on Sonet-4.6 | [readinessProbe.yaml](yaml/app-readinessProbe.yaml)
| Single container pod with liveness/readiness probes and resourse limits| Create a k8s Pod manifest "app-resource", running container "app" using image "gcr.io/kuar-demo/kuard-amd64:1", exposing "http" port 8080, with livenessProbe checking "/healthy" on port 8080 (start after 5s, check every 10s, timeout 1s, fail after 3 attempts), and readinessProbe checking "/ready" on port 8080 (check every 2s, no initial delay, mark ready after 1 success, fail after 3 attempts), with resource requests cpu "100m" memory "128Mi" and limits cpu "1000m" memory "256Mi". Return only valid indented yaml. | Generated by GPT-5.3, verified on Sonet-4.6 | [app-resources.yaml](yaml/app-resources.yaml)
| Single container pod with liveness/readiness probes and volume mount | Create a k8s Pod manifest "app-volume", running container "app" using image "gcr.io/kuar-demo/kuard-amd64:1", exposing "http" port 8080, with livenessProbe checking "/healthy" on port 8080 (start after 5s, check every 10s, timeout 1s, fail after 3 attempts), and readinessProbe checking "/ready" on port 8080 (check every 2s, no initial delay, mark ready after 1 success, fail after 3 attempts), mount a volume "data" into container path "/data", where the volume "data" is backed by hostPath "/var/lib/app". Return only valid indented YAML. | Generated by GPT-5.3-mini,verified on Sonet-4.6 | [app-volumeMounts.yaml](yaml/app-volumeMounts.yaml)
| Single Container Pod with Env from Secret| Create a k8s Pod manifest "app-secret-env", running container "mycontainer" using image "redis", inject environment variables SECRET_USERNAME and SECRET_PASSWORD from Secret "mysecret1" (keys "username" and "password" respectively), and set restart policy so the Pod will not restart automatically after it stops (Never). Return only valid indented YAML. | Generated by GPT-5.3-mini, verified on Sonet-4.6 | [app-secret-env.yaml](yaml/app-secret-env.yaml)
| Multi Container Pod with custom command| Create a k8s Pod manifest "app-two-containers", with an emptyDir volume named "html", running two containers: first container "1st" using image "nginx" mounting volume "html" at "/usr/share/nginx/html", and second container "2nd" using image "debian" mounting the same volume at "/html" and running a loop that continuously appends current date to "/html/index.html" every 1 second. Return only valid indented YAML. | Generated by GPT-5.3-mini,verified on Sonet-4.6 | [app-multicontainer.yaml](yaml/app-multicontainer.yaml)
| Job | Create a k8s Job manifest "app-job", running a pod template that runs once, does not restart after completion or failure, and does not retry failed runs. Define volume "data-input" backed by GCE Persistent Disk "glow-data-disk-200" with ext4 filesystem, and temporary volume "data-output" that exists only for the lifetime of the pod. Add init container "init" using image "google/cloud-sdk:275.0.0-alpine" that syncs data from "gs://glow-sportradar/" into "/data/input". Add main container "processor" using image "glowtools/soccer:latest-4e4254a" that lists files in "/data/input" in long format, mounts "data-input" at "/data/input" and "data-output" at "/data/output", with postStart handler writing start message into "/data/input/message" and preStop handler writing stop message into "/data/output/message". Return only valid indented YAML.| Generated by GPT-5.3-mini, verified on Sonet-4.6 | [app-job.yaml](yaml/app-job.yaml)
| Cron | Create a k8s CronJob manifest "app-cronjob" that runs every 5 minutes, creating a Job that runs a container "hello" using image "bash" and executes the command "echo Hello world", with restart policy set so the container only restarts if it fails. Return only valid indented YAML. | Generated by GPT-5.3-mini, verified on Sonet-4.6 | [yaml/app-cronjob.yaml](app-cronjob.yaml)